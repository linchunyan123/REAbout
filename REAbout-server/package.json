{
  "name": "reabout-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "dev": "node --env-file=.env --watch src/server.js",
    "start": "node --env-file=.env src/server.js",
    "db:migrate": "node --env-file=.env src/migrate.js",
    "test": "node --test",
    "test:integration": "node --env-file=.env scripts/integration.js"
  },
  "dependencies": { "express": "^5.1.0", "pg": "^8.16.3", "helmet": "^8.1.0" }
}
