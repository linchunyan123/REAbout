{
  "name": "focusboard-fullstack",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm ci --prefix REAbout && npm ci --prefix REAbout-server",
    "dev": "node scripts/dev.mjs",
    "build": "npm run build --prefix REAbout",
    "start": "npm start --prefix REAbout-server",
    "db:migrate": "npm run db:migrate --prefix REAbout-server",
    "test": "npm test --prefix REAbout-server && npm test --prefix REAbout && npm run lint --prefix REAbout",
    "test:integration": "npm run test:integration --prefix REAbout-server"
  }
}
